{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "List to file",
            "type": "shell",
            "command": "node ${workspaceFolder}/index.js --dry-run > output.json",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}